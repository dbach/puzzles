# Puzzle helpers
#
# commands for retrieving puzzle sites for offline use

grab-project-euler:
	wget -N -r -l 1 -k "http://mathschallenge.net/index.php?section=project&ref=view"

grab-ruby-quiz:
	wget -N -r -l 1 -k "http://www.rubyquiz.com/"

